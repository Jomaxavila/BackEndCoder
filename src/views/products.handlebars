<div>
  <nav class="navbar navbar-expand-md bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Navbar</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="#">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Link</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Link</a>
          </li>
        </ul>
        <ul class="navbar-nav">
          <li class="nav-item user-nav">
            <a class="nav-link m-1">{{user.name}}</a>
          </li>
          <li class="nav-item">
            <form action="/api/sessions/logout" method="POST">
              <button type="submit" class="btn btn-sm btn-danger m-2">Logout</button>
            </form>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#"><i class="fas fa-shopping-cart m-2"></i></a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
</div>
  <h1 class="m-5 text-center">Lista de productos con paginación</h1>
  <section class="mt-6">
    <div class="container">
      <div class="row justify-content-center">
        {{#each products}}
          <div class="mt-5 col-md-4">
            <div class="card mb-4 h-100">
              <img
                src="{{thumbnail}}"
                style="width: 150px; height: 200px; object-fit: contain;"
                class="img-fluid rounded mx-auto d-block mt-3 mx-auto my-auto"
                alt="Product Image"
              />
              <div class="card-body text-center">
                <h5 class="card-title">{{title}}</h5>
                <p class="card-text">{{description}}</p>
                <p class="card-text">{{category}}</p>
                <p class="card-text">$USD {{price}}</p>
                <form action="/products/addToCart/{{id}}" method="POST">
                  <button type="submit" class="btn btn-success m-2">Agregar al
                    carrito</button>
                </form>
              </div>
            </div>
          </div>
        {{/each}}
      </div>
    </div>
  </section>
  <div class="pagination justify-content-center">
    {{#if hasPrevPage}}
      <a href="/products?page={{prevPage}}"><button
          class="btn btn-secondary m-5"
        >Anterior</button></a>

    {{/if}}
    {{#if hasNextPage}}
      <a href="/products?page={{nextPage}}"><button
          class="btn btn-secondary m-5"
        >Siguiente</button></a>

    {{/if}}
  
  </div>
</div>