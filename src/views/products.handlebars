<div>
  <nav class="navbar navbar-expand-md bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="#"> 📷 Collection Cameras </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
           <li class="nav-item">
            <a class="nav-link" href="/chat">Chat Live</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/products">Back Home</a>
          </li>
        </ul>
        <ul class="navbar-nav">
          <li class="nav-item user-nav">
           <p class="card-text m-3"> {{user}}</p>
          </li>
          <li class="nav-item">
            <form action="/api/session/logout" method="POST">
              <button type="submit" class="btn btn-sm btn-danger m-2">Logout</button>
            </form>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/cart"><i class="fas fa-shopping-cart m-2"></i></a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
</div>
  <h1 class="m-5 text-center">Lista de productos con paginación</h1>
  <section class="mt-6">
    <div class="container">
      <div class="row justify-content-center">
        {{#each products}}
          <div class="mt-5 col-md-5">
            <div class="card mb-4 h-100">
              <img
                src="{{thumbnail}}"
                style="width: 150px; height: 200px; object-fit: contain;"
                class="img-fluid rounded mx-auto d-block mt-4 mx-auto my-auto"
                alt="Product Image"/>
              <div class="card-body text-center">
                <h5 class="card-title">{{title}}</h5>
                <p class="card-text">{{description}}</p>
                <p class="card-text"> Stock {{quantity}}</p>
                <p class="card-text">$usd {{price}}</p>
                <button id = "addToCart" class="btn btn-success m-2 add-to-cart-button" data-cid = "{{../cartIdUser}}" data-productid="{{_id}}">Agregar al carrito</button>        
              </div>
            </div>
          </div>
        {{/each}}
      </div>
    </div>
  </section>
  <div class="pagination justify-content-center">
    {{#if hasPrevPage}}
      <a href="/products?page={{prevPage}}"><button
          class="btn btn-secondary m-5"
        >Anterior</button></a>

    {{/if}}
    {{#if hasNextPage}}
      <a href="/products?page={{nextPage}}"><button
          class="btn btn-secondary m-5"
        >Siguiente</button></a>

    {{/if}}
  
  </div>
</div>

<script src="/js/products.js"></script>