<div>
  <nav class="navbar navbar-expand-md bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="#"> 📷 Collection Cameras </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" href="/chat">Chat Live</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/products">Go back</a>
          </li>
        </ul>
        <ul class="navbar-nav">
          <li class="nav-item user-nav">
            <a class="nav-link m-1">{{user}}</a>
          </li>
          <li class="nav-item">
            <form action="/api/session/logout" method="POST">
              <button type="submit" class="btn btn-sm btn-danger m-2">Logout</button>
            </form>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/cart"><i class="fas fa-shopping-cart m-2"></i></a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
</div>

<h1 class="m-5 text-center">Comprando productos</h1>
<section class="mt-6">
  <div class="container">
    <table class="table table-bordered text-center align-middle">
      <thead>
        <tr>
          <th>Producto</th>
          <th>Descripción</th>
          <th>Imagen</th>
          <th>Precio</th>
          <th>Cantidad</th>
          <th>Acciones</th>
        </tr>
      </thead>

      {{#each cartProducts}}
        <tr>
          <td>{{product.title}}</td>
          <td>{{product.description}}</td>
          <td><img src="{{product.thumbnail}}" alt="Thumbnail" style="width: 50px; max-height: 50px; object-fit: contain;" /></td>
          <td>$USD {{product.price}}</td>
          <td>
            <input type="number" value="{{quantity}}" min="1" max="5" onchange="updateQuantity('{{product._id}}', this.value)">
          </td>
          <td>
            <button  id ="deleteProd" class="btn btn-danger btn-sm" data-productid="{{../_id}}" data-cid = "{{../cartIdUser}}" >Eliminar</button>
          </td>
        </tr>
      {{/each}}

    </table>

    <h3 class="text-center">Total: $USD {{cartTotalAmount}}</h3>
    <button id="purchaseCart" class="btn btn-success m-2 add-to-cart-button" data-cid="{{userCart}}">Comprar</button>
  </div>
</section>

<script src="/js/cart.js"></script>
